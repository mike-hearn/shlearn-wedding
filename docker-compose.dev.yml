version: "3.3"
services:
  web:
    build: .
    user: root
    expose:
      - "4200"
    ports:
      - "49159:49159"
    volumes:
      - ${PWD}:${PWD}
    working_dir: ${PWD}
    networks:
      - web
    command: npm run start

networks:
  web:
    external:
      name: traefik_default
